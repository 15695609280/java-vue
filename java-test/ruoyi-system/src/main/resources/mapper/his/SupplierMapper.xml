<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.his.mapper.SupplierMapper">

    <resultMap type="Supplier" id="SupplierResult">
        <id property="supplierId" column="supplier_id" />
        <result property="supplierName" column="supplier_name" />
        <result property="contact" column="contact" />
        <result property="phone" column="phone" />
        <result property="address" column="address" />
        <result property="status" column="status" />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"  column="create_time"  />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"  column="update_time"  />
        <result property="remark"      column="remark"       />
    </resultMap>

    <sql id="selectSupplierVo">
        select t.supplier_id, t.supplier_name, t.contact, t.phone, t.address, t.status
        from his_supplier t
    </sql>

    <select id="selectSupplierList" parameterType="com.ruoyi.his.domain.Supplier" resultMap="SupplierResult">
        <include refid="selectSupplierVo"/>
        <where>
            <if test="supplierName != null and supplierName != ''"> AND t.supplier_name like concat('%', #{supplierName}, '%')</if>
        </where>
        order by t.supplier_id
    </select>

    <select id="selectSupplierBySupplierId" parameterType="Long" resultMap="SupplierResult">
        <include refid="selectSupplierVo"/>
        where t.supplier_id = #{supplierId}
    </select>

    <insert id="insertSupplier" parameterType="com.ruoyi.his.domain.Supplier" useGeneratedKeys="true" keyProperty="supplierId">
        insert into his_supplier
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="supplierName != null and supplierName != ''">supplier_name,</if>
            <if test="contact != null and contact != ''">contact,</if>
            <if test="phone != null and phone != ''">phone,</if>
            <if test="address != null and address != ''">address,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="remark != null">remark,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="supplierName != null and supplierName != ''">#{supplierName},</if>
            <if test="contact != null and contact != ''">#{contact},</if>
            <if test="phone != null and phone != ''">#{phone},</if>
            <if test="address != null and address != ''">#{address},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="remark != null">#{remark},</if>
        </trim>
    </insert>

    <update id="updateSupplier" parameterType="com.ruoyi.his.domain.Supplier">
        update his_supplier
        <trim prefix="SET" suffixOverrides=",">
            <if test="supplierName != null and supplierName != ''">supplier_name = #{supplierName},</if>
            <if test="contact != null and contact != ''">contact = #{contact},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="address != null and address != ''">address = #{address},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where supplier_id = #{supplierId}
    </update>

    <delete id="deleteSupplierBySupplierId" parameterType="Long">
        delete from his_supplier where supplier_id = #{supplierId}
    </delete>

    <delete id="deleteSupplierBySupplierIds" parameterType="String">
        delete from his_supplier where supplier_id in
        <foreach item="supplierId" collection="array" open="(" separator="," close=")">
            #{supplierId}
        </foreach>
    </delete>
</mapper>
